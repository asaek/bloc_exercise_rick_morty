import 'package:flutter/material.dart';

class TitleImageRick extends StatelessWidget {
  const TitleImageRick({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Image.asset('assets/images/title.png', width: 250, height: 80);
  }
}
